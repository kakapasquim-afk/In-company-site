# Memória do Projeto — Unipar In Company

Registro contínuo de decisões, aprendizados e histórico para uso em sessões futuras. Este arquivo deve crescer ao longo do desenvolvimento; não remova entradas anteriores sem registrar a substituição em "Mudanças de Direção".

## Decisões

### 2026-09-07 — Seção "Presença e unidades": estrutura final combinada (PRESENÇA mapa+lista+filtros → NOSSAS UNIDADES com 10 cards abaixo)
- **Pedido do usuário:** corrigir a seção para que os DOIS blocos coexistam obrigatoriamente: **BLOCO A "Presença"** (título, descrição, mapa + lista de cidades + filtros) e **BLOCO B "Nossas unidades"** (título "Nossas unidades" + subtítulo "Conheça nossos campi" + 10 cards com imagem/cidade/campus/endereço/CTA), com o Bloco B **abaixo** do bloco do mapa/lista — nunca ao lado. Hierarquia obrigatória: PRESENÇA → MAPA+LISTA → NOSSAS UNIDADES → 10 CARDS. Contagem: Umuarama 3, Cianorte 1, Toledo 2, Guaíra 1, Paranavaí 1, Cascavel 1, Francisco Beltrão 1 = **10**.
- **O que foi feito:**
  - `index.html`: após `.units__filters-row`, adicionados o `.units__heading` ("Nossas unidades" / "Conheça nossos campi") e o `.units__grid` com os **10 `.unit-card` restaurados** (imagem, cidade, campus, endereço com pin, CTA "Mais informações" → link oficial `https://www.unipar.br/unidades/<slug>/<campus>/` com `target="_blank"`). O bloco mapa+lista+filtros, a estrutura `.units__layout` e o `.section-title` permaneceram **intactos** (fonte de verdade da hierarquia: mapa/lista separados dos cards).
  - `css/styles.css`: adicionados `.units__heading`, `.units__title` e `.units__grid` (1col → 2col 640px → 3col 1024px, gap 20px). O CSS `.unit-card` (que estava **dormente**) passou a ser consumido pela home, sem alteração no seu estilo.
  - `js/main.js`, `js/map.js` e `data/unidades.js`: **intactos** — a interação `unit:select` (lista lateral ↔ chips ↔ marcadores) continua funcionando e não toca nos cards.
- **Validação:** `node --check` (JS) OK; dump DOM headless — 10 `.unit-card`, 1 SVG do mapa + 7 marcadores, 7 cidades na lista, 8 chips de filtro, heading e grid presentes, ordem DOM **mapa → lista → filtros → heading → grid**; medição de layout via CDP em **375 / 768 / 1440 px** — sem overflow horizontal, cards **abaixo** do bloco mapa+lista, grid 1col (mobile) / 2col (tablet) / 3col com última linha 1 card (desktop, 3+3+3+1); teste de interação — clicar em chip/lista/cidade destaca o marcador correspondente no mapa e sincroniza o ativo **sem remover nenhum card**.

### 2026-09-07 — Seção "Presença e unidades" corrigida na estrutura definitiva (mapa | cidades + filtros abaixo, sem cards na home)
- **Pedido do usuário (correção definitiva):** substituir a estrutura "mapa sozinho + cards abaixo" pela definitiva: **mapa e lista de cidades lado a lado** (mapa à esquerda, lista à direita) e **filtros abaixo** do bloco mapa+cidades. A lista lateral deve conter **somente os nomes** dos destinos (7 cidades, uma vez cada) — **sem fotos, endereços, cards ou botão** "Ver unidades". Filtros em chips (podem quebrar linha no desktop). Dados das 10 unidades preservados para futuras páginas `unidades/<cidade>.html`. Executar a troca sem redesenhar: ler `specs/design.md` antes, não alterar outras seções, não duplicar/criar imagens, só HTML/CSS/JS.
- **O que foi feito:**
  - `index.html`: `#unidades` reestruturada — `.units__layout` (grid `1fr → 1.4fr 1fr` ≥1024px) com `.units__map-card` (esquerda) + `.units__cities-card` (direita, lista de 7 `.units__city-item` com `data-city`); abaixo, `.units__filters-row` com `.units__filter` e chips `.units__btn` ("Todas as cidades" `is-active` + 7 cidades). Removidos `.units__heading` e os 10 `.unit-card` da home.
  - `css/styles.css`: restaurados `.units__layout`, `.units__cities-card`, `.units__cities`, `.units__city-item` (hover `--red-50`, ativo borda esquerda `--primary`), `.units__filters-row`, `.units__filter` (flex wrap center), `.units__btn` (pill, ativo `--primary`); `.units__map-card` voltou a ser grid child (`min-width: 0`); CSS de `.unit-card` mantido **dormente** para futuras subpáginas.
  - `js/main.js`: restaurado o bloco de seleção — `.units__city-item` + `.units__btn` disparam `unit:select` e sincronizam `.is-active` (lista e filtros); ouvinte de `unit:select` mantém o estado. `js/map.js` e `data/unidades.js` **intactos** (marcadores e 10 campi preservados).
- **Validação:** `node --check` (`js/main.js`, `js/map.js`) OK; dump DOM headless — **todas as checagens PASS** (mapa + 7 marcadores, lista lateral com 7 cidades únicas 1x cada, sem `<img>`/cards/heading/grid na seção, filtros com 8 chips e "Todas as cidades" ativo, filtros **abaixo** do bloco mapa+cidades, main.js/map.js com interação `unit:select`, `data/unidades.js` com 10 unidades, pasta `img` inalterada); medição de layout 1440px (grid 2 col: mapa 764px esquerda + cidades 546px direita no mesmo topo; filtros abaixo; sem overflow) e mobile ~496px (mapa → cidades → filtros empilhados; sem overflow).

### 2026-09-07 — Seção "Presença e unidades" corrigida (mapa sozinho + 10 cards das unidades abaixo)
- **Pedido do usuário:** correção da reorganização anterior — **restaurar as imagens e informações das unidades** que haviam sido removidas. Estrutura exigida: título → **mapa sozinho** no topo → **logo abaixo** a área das unidades com cards. **Unidades NÃO ao lado do mapa** e sem cards sobrepostos. Restaurar o que já existia (imagens do projeto, nomes de cidades, campi, endereços, links, textos), total **10 unidades/campi** (Umuarama Campus I Sede / II Clínica Escola Veterinária / III Tiradentes, Cianorte, Toledo Campus I e II, Guaíra, Paranavaí, Cascavel, Francisco Beltrão). Não alterar header/hero/footer/mapa. Usuário confirmou: **"Mapa sozinho + cards abaixo"** (remover lista de cidades e filtros).
- **O que foi feito:**
  - `index.html`: a lista de cidades (`.units__cities-card` dentro de `.units__layout`) e a linha de filtros (`.units__filters-row`) foram **removidas**; `.units__map-card` voltou a ser o único bloco do topo. Adicionados `.units__heading` ("Nossas unidades") e `.units__grid` com os **10 `.unit-card`** (imagem, cidade, campus, endereço com pin, CTA "Ver unidades" → link externo oficial `https://www.unipar.br/unidades/<slug>/<campus>/` com `target="_blank"`).
  - `css/styles.css`: removidos `.units__layout`, `.units__cities-card`, `.units__cities`, `.units__city-item`, `.units__filters-row`, `.units__filter`, `.units__btn`, `.units__side`; `.units__map-card` agora é standalone (`max-width 800px` centralizado, `margin-top 48px`); novo `.units__heading`; `.units__grid` `1col → 2col (640px) → 3col (1024px)`; `.unit-card` (padrão visual existente) mantido.
  - `js/main.js`: bloco de filtros (`.units__city-item`/`.units__btn`/`unit:select`) **removido** por não haver mais UI de filtro; `js/map.js` (SVG, marcadores, tooltip) e `data/unidades.js` **intactos**.
- **Observação:** Campus II de Umuarama tem `imagem: null` em `data/unidades.js` (sem foto própria no disco) — o card reutiliza `img/campus-umuarama.jpg`, como os demais campi de Umuarama.
- **Validação:** `node --check` (`js/main.js`, `js/map.js`) OK; dump DOM headless **21/21 ALL PASS** (mapa + nota, heading, 10 cards, 10 imagens existentes em disco, ordem mapa → heading → grid, sem resíduos de filtros); medição de layout em 375px e 1440px — sem overflow horizontal, mapa sozinho/centralizado no topo, grid 1col no mobile e 3col no desktop, cards abaixo do mapa.

### 2026-09-07 — Seção "Presença e unidades" reorganizada (lista de cidades + filtros abaixo do mapa)
- **Pedido do usuário:** reorganizar a seção sem redesenhar. Remover os cards com foto/endereço da lateral do mapa; deixar na lateral **somente os nomes das cidades** (7 cidades únicas); mover os **filtros para abaixo** do bloco mapa+cidades; não apagar nenhum dado das unidades (serão usados nas futuras subpáginas `unidades/<cidade>.html`).
- **O que foi feito:**
  - Lateral direita do mapa virou um card de **lista de cidades** (`.units__cities-card` com `.units__city-item`): texto Manrope 600, hover fundo `--red-50`, item ativo com borda esquerda `--primary` + cor `--primary` + peso 700. Cidades únicas, uma única vez, na ordem original.
  - Os **cards com foto/endereço/CTA** (`.unit-card` / `.units__grid`) foram **removidos da lateral** — os dados dos 10 campi permanecem intactos em `data/unidades.js` (10 unidades, 7 cidades) para as futuras páginas.
  - Os **filtros** (`.units__filter` + `.units__btn`) foram movidos para uma linha própria abaixo do bloco mapa+cidades (`.units__filters-row`), mantendo o funcionamento.
  - Grid desktop passou de `1.1fr 1fr` para `1.4fr 1fr` (mapa dominante); mobile empilha mapa → cidades → filtros.
- **Arquivos alterados:** `index.html` (`.units__layout` reestruturado: `.units__map-card` + `.units__cities-card`; `.units__filters-row` com filtros; cards `.unit-card` removidos), `css/styles.css` (novos `.units__cities-card`, `.units__cities`, `.units__city-item`, `.units__filters-row`; `.units__layout` ajustado), `js/main.js` (lista de cidades e filtros compartilham o mesmo estado ativo via `unit:select`).
- **Mantidos intactos:** `js/map.js` (SVG, marcadores, coordenadas), `data/unidades.js` (10 campi), `data/parana.geojson`, demais seções/header/footer.
- **Validação:** `node --check` (`js/main.js`, `js/map.js`) OK; `data/unidades.js` avaliado — 10 unidades, 7 cidades únicas; estrutura HTML conferida (lista completa, 1 item por cidade, filtros abaixo do layout).

### 2026-09-07 — Seção "Presença e unidades": mapa antigo restaurado (estilo da referência `unipar-main`)
- **Pedido do usuário:** restaurar o mapa antigo da seção de unidades conforme a referência visual antiga (implementada em `unipar-main/components/sections/units.tsx` com react-simple-maps), mantendo **tudo o resto intacto** (cards, nomes, imagens, endereços, dados, ordem, interação filtro↔mapa, responsividade).
- **O que foi restaurado:** cabeçalho voltou a "Presença e **unidades**" + subtítulo antigo ("Localidades identificadas no material institucional da Unipar In Company."); nota sob o mapa virou a legenda antiga ("Localização aproximada das unidades no estado do Paraná. Endereços disponibilizados sob consulta."); layout em `.units__layout` — mapa em card à esquerda + filtro/cards à direita (≥1024px; empilha no mobile); estado do Paraná `fill --red-50` + `stroke --primary` 1.5; marcadores minimalistas = halo r10 (`--primary`, `opacity .16`, ativo/hover r12) + pino r5 (`--primary`, traço branco 2); **tooltip** 108×28 rx8 aparece só no hover/:focus/ativo — os **labels sempre visíveis foram removidos**.
- **Arquivos alterados:** `index.html` (título/subtítulo, `.units__layout`, `.units__map-card`, `.units__side`, legenda), `css/styles.css` (`.units__layout`, `.units__side`, `.units__grid` 1/2/2 col, estilos `.units-map__halo/_pin/_tooltip`), `js/map.js` (SVG com halo+pino+tooltip, `aria-pressed` em `setActive`, aria-label antigo).
- **Mantidos intactos:** 7 cards de cidade e imagens (`index.html`), `data/unidades.js` (10 campi), `data/parana.geojson`, coordenadas dos marcadores, `js/main.js` (interação `unit:select`), demais seções/header/footer.
- **Validação:** `node --check` (`js/map.js`, `js/main.js`); dump DOM headless `check-map.js` **20/20** (título/subtítulo/legenda antigos, `.units__layout`/`.units__side`, SVG renderizado, 7 marcadores, 7 tooltips, 0 labels, aria-label, 7 cards, imagens, filtro); medição de layout calculado via `index-measured.html` (viewport 800px: 1 coluna mapa→cards; 1440px: `grid 686×624px` mapa à esquerda + side à direita, cards 2 col); cores conferidas (`--red-50` = `oklch(0.971 0.014 17)` no `.units-map__fill`; halo opacity .16; stroke primary 1.5; tooltip com opacidade transicionada).

### 2026-09-07 — Formulário de contato com envio real via Resend (serverless)
- **Problema:** `js/contact.js` apenas simulava o envio (estado "prepared"); o formulário não enviava nada de fato.
- **Solução (confirmada com o usuário):** integração real com **Resend** por meio de uma **Vercel Serverless Function** (`api/contact.js`), pois o site é estático (sem framework/build). Sem banco de dados.
  - **Segurança:** `RESEND_API_KEY` existe **somente no servidor** (variável de ambiente; `.env` local para testes). Nunca vai ao navegador, ao Git nem às respostas da API. `.env.example` tem só placeholders; `.gitignore` cobre `.env*`.
  - **Configuração (env):** `RESEND_API_KEY`, `RESEND_FROM_EMAIL` (remetente autorizado no Resend; fallback `onboarding@resend.dev` do sandbox) e `RESEND_TO_EMAIL` (default `celso.ferrari@prof.unipar.br`).
  - **E-mail:** assunto `Novo contato pelo site — [Nome]`; HTML profissional em table (banda vermelha `#c8102e`, campos Nome/Empresa/E-mail/Telefone/Mensagem) + rodapé "Esta mensagem foi enviada através do formulário de contato do site." e `reply_to` = e-mail do visitante.
  - **Validação dupla (cliente + servidor):** nome/e-mail/mensagem obrigatórios; e-mail com regex; mensagem ≥10 e ≤5000; e-mail ≤254; nome ≤200; empresa ≤300; telefone ≤40. Sanitização remove `<tags>` + `escapeHtml` bloqueiam injeção no e-mail.
  - **Anti-spam sem banco:** honeypot `website` oculto (envio silencioso se preenchido) + rate limit em memória (5 req/IP/10min, por instância da function — limitação documentada).
  - **UX preservada:** sem reload; botão desabilitado com spinner "Enviando..."; sucesso limpa o form e mostra "Mensagem enviada com sucesso! Entraremos em contato em breve."; erro mantém os dados e mostra "Não foi possível enviar sua mensagem. Tente novamente."; erros por campo sob os inputs. Visual/layout intocados.
- **Arquivos novos:** `api/contact.js`, `.env.example`, `.gitignore`, `package.json` (mínimo, `engines.node >= 18`, sem deps). **Alterados:** `js/contact.js` (envio real via `fetch` a `/api/contact`), `index.html` (honeypot), `css/styles.css` (estados de erro/honeypot).
- **Validação:** `node --check` OK (`api/contact.js`, `js/contact.js`, `data/unidades.js`). Harness local com `fetch` mockado (fora do projeto) — **42/42 passam**: sucesso completo, escape HTML/anti-injeção, erros 400 (obrigatórios, e-mail inválido, mensagem curta, empresa longa), cap de mensagem 5000, honeypot sem chamar o Resend, GET→405, OPTIONS→204, body inválido→400, falha do Resend→500, sem chave→500 sem chamar o Resend, 6º envio→429, resposta nunca vaza a chave.
- **Teste real:** definir as vars no deploy (Vercel: Settings > Environment Variables) ou em `.env` com `vercel dev`; domínio verificado no Resend (ou sandbox `onboarding@resend.dev`, que só entrega a e-mails verificados).
- **Ajuste de robustez (validação):** `sanitize()` em `api/contact.js` e `js/contact.js` agora retorna `""` para valor `null`/`undefined`. Antes, um payload sem a chave `name` virava a string `"undefined"` (truthy) e passava na validação; agora o campo ausente é rejeitado com 400 no servidor e bloqueado no cliente. Harness revalidado: **53/53 (API, Resend mockado)** e **29/29 (cliente, DOM simulado)** — destinatário `celso.ferrari@prof.unipar.br`, todos os campos, `reply_to`, assunto, honeypot e rate limit confirmados.

### 2026-09-07 — Imagens das unidades padronizadas (campus-<cidade>.<ext>) + fotos novas
- **Fotos novas do usuário localizadas** no `img/` (mesmo lote, 717×538): `Unipar-cascavel.webp` (Cascavel), `paranavaí unipar.jpg` (Paranavaí), `Guaíra.jpg` (Guaíra) e `Sede_umuarama.jpg` (Umuarama — **não solicitada**, ficou sem uso).
- **Renomeações** (mantendo a extensão original, padrão `campus-<cidade>.<ext>`):
  - `Unipar-cascavel.webp` → `campus-cascavel.webp` (nova foto usada no card)
  - `paranavaí unipar.jpg` → `campus-paranavai.jpg` (nova foto usada no card — antes o card de Paranavaí ficava **sem imagem**)
  - `Guaíra.jpg` → `campus-guaira.jpg` (nova foto usada no card de Guaíra)
  - `campus-umuarama-3.png` → `campus-umuarama.png` (padronização; era a foto usada no card)
  - `parking`/bloco Umuarama — **foto trocada** (sub-pedido do usuário): card passou a usar a **nova foto de Umuarama** (`campus-umuarama.jpg`, idêntica a `Sede_umuarama.jpg` — mesmo hash SHA256). A foto antiga `campus-umuarama.png` ficou **sem uso** (mantida, não apagada). Referências de Umuarama em `index.html` e `data/unidades.js` (Campus I e III) apontam para `img/campus-umuarama.jpg`.
- **Descoberta importante:** `img/campus-cascavel.png` e `img/campus-umuarama-2.png` **nunca existiram** no projeto (referências pré-existentes quebradas). O card de Cascavel quebrava antes; agora usa `campus-cascavel.webp`. Em `data/unidades.js`, o Campus II de Umuarama passou a `imagem: null` (sem foto no disco).
- **Referências atualizadas:** `index.html` (7 cards, src/ALT) e `data/unidades.js`. ALT padrão: `Campus Unipar <Cidade>`. CSS/JS não referenciam imagens de campus. Textos visíveis mantêm grafia com acento (Guaíra, Paranavaí, Francisco Beltrão); nomes técnicos sem acento.
- **Imagens antigas mantidas (sem uso):** `campus-guaira.png`, `campus-guaira-2.png`. Nada foi apagado.
- **Validação:** 7 referências em `index.html` conferidas — todos os arquivos existem; `node --check data/unidades.js` OK; grep não encontra nomes antigos (`campus-umuarama-3`, `campus-umuarama-2`, `campus-guaira.png`, `campus-cascavel.png`, `Unipar-cascavel`, `paranavaí unipar`, `Guaíra.jpg`).

### 2026-09-07 — Seção de unidades deduplicada (1 card por cidade + dados preservados)
- **Problema:** a home listava 10 cards de unidade; Umuarama aparecia 3× (Campus I, II, III) e Toledo 2× (Campus I, II), gerando duplicidade na seção "Nossas Unidades".
- **Solução (confirmada com o usuário):**
  - **Um card por cidade** (7 cidades), mantendo a **ordem atual do `index.html`**: Umuarama, Cianorte, Toledo, Guaíra, Paranavaí, Cascavel, Francisco Beltrão (não reordenada).
  - Cards com **conteúdo de nível cidade**: título "Unidade em <Cidade>" e endereço representativo (campus principal).
  - **Paranavaí ficou sem imagem** (não existe `img/campus-paranavai.png`; decisão do usuário — aguardar foto real).
  - Links dos cards agora apontam para **futuras subpáginas** `unidades/<cidade>.html` (ainda não criadas), com CTA "Ver unidades" e navegação interna (sem `target="_blank"`).
  - **Dados dos 10 campi individuais NÃO foram apagados**: foram preservados em `data/unidades.js` (`window.UNIDADES`), prontos para alimentar as subpáginas e/ou o mapa.
  - Mapa e filtro da seção **não foram alterados** (mostram localização geográfica das cidades).
- **Validação:** `index.html` re-lido — 7 cards, 6 imagens (Paranavaí sem), sem referência quebrada `campus-paranavai.png`; `data/unidades.js` validado com `node --check`.

### 2026-09-06 — Logo do header corrigida (topo do símbolo cortado)
- **Problema:** no header, o topo do símbolo da logo aparecia cortado. Causa: `.brand-logo` (caixa `224×48`, aspecto 4.67) com `object-fit: cover` sobre a imagem `1920×1080` (aspecto 1.78) → a escala era limitada pela largura (224/1920 = 0.1167) e a altura excedente (≈126 vs 48) era cortada 39px em cada lado; o conteúdo útil (símbolo + texto; bbox branco do símbolo em `y[302..777]`, aspecto ≈3.705) começava em y=−3.8 → o topo do símbolo era perdido.
- **Solução:** ajustar a **largura** do box para aproximar o aspecto do conteúdo, deixando o `object-fit: cover` cortar apenas o excesso mínimo:
  - Estado normal: `.site-header .brand-logo { width: 178px; }` (caixa 178×48, aspecto 3.708 ≈ 3.705 do conteúdo) → conteúdo entra com folga de ~2px acima e ~5px abaixo; símbolo 100% visível.
  - Estado ao rolar (`is-scrolled`): `.site-header__logo-link.is-scrolled .brand-logo { width: 148px; height: 40px; }` (antes 192×40) → aspecto 3.7, folga ~2px acima e ~7px abaixo.
  - **Escopo restrito ao header.** As regras globais `.brand-logo` (entrance `320×80`/`384×96` e footer) não foram alteradas.
- **Verificação:** DOM headless (Edge) confirma box real `178×48` (normal) e `148×40` (scrolled), imagem `1920×1080` carregada, `object-fit: cover`. Análise de pixels do screenshot: contagem branca por linha dentro dos boxes — normal: conteúdo `y[42..83]` (folgas 2px/5px); scrolled: `y[116..147]` (folgas 2px/7px). Nenhum glyph encostou nas bordas do box (sem corte, com respiro acima e abaixo).
- **Regra derivada:** qualquer reescalonamento futuro da logo do header deve manter o box com aspecto ≈ **3.7** (conteúdo da logo) para não cortar o símbolo.

### 2026-09-06 — Mapa do Paraná corrigido (seção "Presença e unidades")
- **Problema:** o card do mapa ficava em branco. Causas: (1) `js/map.js` usava `fetch("data/parana.geojson")`, que **falha ao abrir a página por `file://`** (sem servidor local) e o `.catch()` anulava tudo silenciosamente; (2) risco adicional de orientação incorreta (eixo Y da projeção sem inversão → Paraná de cabeça para baixo).
- **Solução:** `js/map.js` agora **embute os dados pré-computados** — caminho SVG do estado (2851 pontos, Web Mercator com inversão de Y → norte em cima, fit em viewBox 800×600 com margem 40) e as coordenadas dos 7 marcadores no mesmo espaço — **sem nenhum fetch**. Renderiza por `file://` e por servidor.
- **Estética:** contorno vermelho (`--primary`), preenchimento claro (`--red-50`), 7 marcadores vermelhos (halo `opacity:0.16` + círculo interno), tooltip ao focar/passar o mouse e sincronização com a lista lateral (eventos `unit:select`/`unit:clear`) — tudo preservado.
- **Reuso:** `data/parana.geojson` continua como fonte única (agora consumido em tempo de geração); nenhum asset novo criado; cidades reutilizadas das 7 unidades já registradas (Umuarama, Cianorte, Paranavaí, Toledo, Cascavel, Guaíra, Francisco Beltrão).
- **Validação:** `node --check` OK; DOM headless (Edge) renderiza `svg` + `path` + 14 `circle` (7 marcadores); teste point-in-polygon confirma os 7 marcadores dentro do contorno do Paraná; análise de pixels do screenshot confirma preenchimento claro (43%) e contorno/marcadores vermelhos. Sem overflow horizontal (SVG `width/height:100%`, `aspect-ratio 4/3` no card).
- **Fora do escopo:** nada alterado em `index.html`, `css/styles.css`, páginas de projeto ou outras seções (HTML/CSS do card intactos).

### 2026-09-06 — Favicon corrigido (In Company)
- **Problema:** o favicon da In Company (`img/favicon-in-company.png.png`, 1672×940) era usado diretamente como favicon e aparecia **esticado** na aba do navegador (proporção 16:9 em espaço 1:1).
- **Solução adotada:** analisou-se a imagem de origem — nela já existe **apenas o símbolo branco da Unipar centralizado sobre fundo vermelho** (bbox do símbolo ≈ 663×612; sem texto "in Company by UNIPAR", sem pixels escuros). Gerou-se a partir dela:
  - `img/favicon-in-company.png` — **32×32 px**, proporção 1:1 (recorte quadrado 849×849 centralizado no símbolo com margem ~14%).
  - `img/favicon-in-company-16.png` — **16×16 px**, mesma origem.
  - O recorte preserva o símbolo sem deformação (redimensionamento uniforme 1:1, `HighQualityBicubic`); fundo vermelho sólido `RGB(242,2,1)` (cor dominante do fundo original).
- **`<head>` configurado** em `index.html` e `projetos/_head.html` (fonte da verdade) + 9 páginas geradas: dois `<link rel="icon">` com `sizes="32x32"` e `sizes="16x16"`.
- **Nenhuma outra parte do site foi alterada.** O arquivo original `favicon-in-company.png.png` foi preservado (não modificado).

### 2026-09-06 — Faixa de parceiros no Hero (confirmada com o usuário)
- **Contexto:** foi proposto integrar ao site a ideia da "hero + área de parceiros" da referência React (`ResponsiveHeroBanner`). O componente NÃO deve ser instalado nem convertido para React — serve apenas como referência estrutural. Stack permanece **HTML + CSS + JavaScript puro** (sem React/TypeScript/Tailwind/shadcn).
- **Estrutura do hero:** MANTER o hero atual (`index.html` seção `#inicio`: fundo branco, grid 2 colunas, imagem à direita). NÃO substituir por um layout full-screen com imagem de fundo.
- **Área de parceiros no hero:** adicionar uma faixa condensada dos **8 logos de parceiros** existentes, logo abaixo do hero (antes da seção `#sobre`).
- **Clicabilidade:** os logos na faixa do hero são **decorativos / NÃO clicáveis** (sem link, sem `href`, sem hover de clique). Isso **substitui** a intenção original ("cada parceiro clicável levando à página") para esta faixa específica. Os parceiros **continuam clicáveis** na seção `#parceiros`/`Organizações` existente, que leva às páginas de projeto.
- **Assets:** reutilizar os mesmos logos já usados em `index.html` (`img/partners/*`). NÃO baixar nem recriar logos. NÃO usar os assets de exemplo da referência.
- **Visual:** a faixa deve ser limpa, discreta, elegante, organizada e responsiva; o conteúdo principal do hero continua sendo o foco. Animações sutis (CSS transitions/keyframes) permitidas; JS apenas se necessário.

### 2026-09-06 — Base de documentação do `unipar-site`
- Foi criada a base de contexto deste diretório: `AGENTS.md`, `specs/design.md` e `memoria.md` (análoga à já existente no projeto irmão `../unipar-main`).
- **Objeto do site:** site **estático** (HTML/CSS/JS puro) que migra o design da implementação original Next.js/Tailwind v4 (`../unipar-main`).
- **Referência visual (Oura):** o print/captura da página da Oura e o arquivo com o link da página da Oura **não foram encontrados** neste projeto. Assim como registrado no projeto irmão, a Oura permanece apenas como intenção de direção futura (clima, hierarquia, minimalismo premium) — **sem clonar**. Base de decisões = site atual.
- **Produto identificado:** Unipar In Company — vertente de educação corporativa da Universidade Paranaense (Unipar), criada em 2023 pela Diretoria de Inovação.
- **Público-alvo:** empresas, cooperativas de crédito (Sicredi, Sicoob, Cresol, Uniprime), indústrias (Gazin, Castelini) e instituições do Paraná.
- **Contatos oficiais (fixos):** e-mail `celso.ferrari@prof.unipar.br`, telefone `(44) 9916-2293`, WhatsApp `554499162293`, sede na Praça Mascarenhas de Moraes 4282, Umuarama/PR.
- **Equipe divulgada:** Prof. Celso Ferrari Júnior (coordenador) e Kayke Pasquim Ferrari (desenvolvimento e tecnologia do site).

### 2026-09-09 — Imagens castelini-5 e castelini-6 lado a lado no projeto Castelini
- **Pedido do usuário:** inserir as imagens existentes `castelini-5.jpg` e `castelini-6.jpg` lado a lado, dentro de uma DIV própria, abaixo do bloco completo de texto do Programa de Formação de Gestores (após a última seção de conteúdo — SECTION 9 "Investir em gestores é investir no futuro da organização" — e antes do CLOSING). As imagens já existiam fisicamente no `img/` e não precisaram ser criadas, baixadas ou substituídas.
- **O que foi feito:**
  - `projetos/_body-castelini.html` e `projetos/castelini.html`: adicionado bloco `<!-- DUO IMAGES -->` com `<div class="case-image-duo reveal">` contendo dois `<figure class="case-image case-image--cover">` com `../img/castelini-5.jpg` e `../img/castelini-6.jpg`. Inserido entre SECTION 9 e CLOSING. Classe `reveal` para animação fade-up existente. ALTs: "Grupo Castelini — Programa de Formação de Gestores".
  - `css/styles.css`: adicionado componente `.case-image-duo` — CSS Grid responsivo (`1fr` → `2col 640px`, `gap: 20px`), reutilizando os estilos existentes de `.case-image` e `.case-image--cover` (border-radius 16px, border, shadow, aspect-ratio 16/10 → 21/10 em desktop, object-fit cover).
- **Imagens 1–4 do Castelini** (castelini-1 a castelini-4): **intactas**, mantidas em suas posições originais.
- **Nenhuma outra seção alterada** — header, hero, mapa, unidades, formulário, footer e demais páginas não foram tocadas.
- **Validação:** caminhos das imagens conferidos (`../img/castelini-5.jpg` e `../img/castelini-6.jpg` existem); template e página gerada em sincronia; CSS consistente com o padrão visual existente.

### 2026-09-09 — Nova seção "Análise e concessão de crédito para pessoa jurídica" na página da Gazin
- **Pedido do usuário (aditivo):** adicionar uma nova seção no projeto da Gazin sobre a formação em Análise e Concessão de Crédito para Pessoa Jurídica, com **4 parágrafos** e, logo abaixo, as **imagens `gazin-9.jpg` e `gazin-10.jpg`** (arquivos existentes do projeto, do lote do curso/aplicação; nenhuma imagem criada, baixada ou substituída). A nova seção entra **imediatamente antes** da seção existente "Educação corporativa que gera desenvolvimento", sem apagar/reordenar nenhum conteúdo posterior (CLOSING, "O projeto em movimento"/vídeo e footer intactos).
- **O que foi feito:**
  - `projetos/_body-gazin.html` e `projetos/gazin.html` (página gerada, mantida em sincronia): inserido bloco `<!-- SECTION CRÉDITO -->` com `.project-block` → `.text-block` (eyebrow "Concessão de crédito", título "Análise e concessão de crédito para pessoa jurídica", 4 parágrafos exatos) + `<div class="case-image-duo reveal">` com dois `<figure class="case-image case-image--cover">` (`../img/gazin-9.jpg` e `../img/gazin-10.jpg`, ALTs "Gazin — Análise e concessão de crédito para pessoa jurídica").
  - **CSS: nenhuma alteração** — reutilizado o componente existente `.case-image-duo` (grid `1fr → 2col 640px`, gap 20px, imagens `16/10 → 21/10` `object-fit: cover`, sombra, border-radius 16px; stack no mobile), mesmo padrão usado na página do Castelini. Nenhuma nova classe/arquivo criado.
  - Imagens `gazin-1` a `gazin-8` e seus usos na página: **intactos**. Vídeo (iframe `youtube-nocookie`, origin unipar.br) e `js/main.js` (reveal): intactos.
- **Observação de diretório:** a pasta indicada pelo usuário como oficial ("In company siteChat") **não existe**; a alteração foi aplicada no projeto em `In company site` (única pasta real do site estático, confirmada com o usuário).
- **Validação:** estrutura DOM conferida em `gazin.html` (nova seção entre SECTION 9 e SECTION 10; 4 parágrafos; duo com as 2 imagens; SECTION 10/CLOSING/VIDEOS/footer no lugar); arquivos `img/gazin-9.jpg` e `img/gazin-10.jpg` existem em disco; referências `gazin-1..10` presentes; vídeo e script intactos.

## Aprendizados

- O site estático usa **HTML5 semântico** + **CSS puro** com tokens em variáveis `oklch` + **JavaScript vanilla** (sem frameworks e sem build para a home).
- As **páginas de projeto** (`projetos/*.html`) são montadas por um script PowerShell (`projetos/build.ps1`) que combina `_head.html` + `_header.html` + `_body-*.html` + `_footer.html`, substituindo `__TITLE__` e `__DESC__`.
- As fontes são carregadas via Google Fonts: **Manrope** (títulos) e **Inter** (corpo).
- Os tokens visuais vivem em `css/styles.css`; o vermelho institucional deriva de Unipar `#c8102e` (`oklch(0.523 0.214 26)`).
- Navegação por âncoras em página única (`/#secao`); header fixo vermelho que encolhe ao rolar.
- O **mapa das unidades** é um SVG inline gerado por `js/map.js` com dados **pré-computados** a partir de `data/parana.geojson` (Web Mercator, norte em cima, viewBox 800×600) — sem fetch, então funciona abrindo `index.html` por `file://`; substitui o `react-simple-maps` do original.
- Formulário de contato validado em `js/contact.js` e enviado por `fetch` à Function serverless `api/contact.js`, que integra com **Resend** (chave só no servidor). Para testes locais use `.env` + `vercel dev`.
- A **entrance screen** aparece 1x por sessão via `sessionStorage`.
- A logo é carregada de URL externa (Vercel Blob) com `mix-blend-mode: screen`; existe cópia local em `img/`.
- A logo do header (imagem 1920×1080 com símbolo+texto centralizados) precisa de caixa com aspecto ≈ **3.7** (ex.: 178×48, 148×40) sob `object-fit: cover` para não cortar o topo do símbolo; caixas 16:9 classicistas (ex.: 224×48) cortam o símbolo.
- Imagens: fotografias usam `object-cover`; pôsteres/artes usam `object-contain` (regra obrigatória). **Exceção (2026-09-14):** fotos panorâmicas muito largas que perderiam conteúdo sob `cover` (ex.: `sescoop-4`, `sescoop-5`) são exibidas inteiras via classe escopada (`.case-image-duo--sescoop`) com `aspect-ratio: auto` + `height: auto` + `object-fit: contain`.

## Problemas e Soluções

- **Referência Oura indisponível:** o print e o link da página da Oura não foram encontrados em `unipar-site` (a única menção à Oura em todo o diretório-fonte é em ocorrências do nome da cidade "Douradina" no conteúdo da Gazin). Solução: documentar o estado atual do site como base e registrar a Oura como referência futura em "Decisões em Aberto" do `specs/design.md`.
- **Conteúdo duplicado entre HTML e templates:** as páginas geradas (`projetos/*.html`) e os `_body-*.html` convivem; alterações devem ser feitas nos templates e depois regeneradas via `build.ps1` para não divergirem.

## Mudanças de Direção

- **2026-09-06:** A faixa condensada de parceiros (o que havia sido pedido como "área de parceiros no hero") **foi removida** após o usuário não gostar do resultado. O `index.html` voltou exatamente ao estado anterior (sem `.hero-partners`); o `css/styles.css` também foi revertido (bloco `.hero-partners` removido). A seção `#parceiros`/`Organizações` existente permanece intacta.
- **2026-09-07:** Fotos das unidades padronizadas para o padrão **`img/unidade-<cidade>-<nn>.<ext>`** (numeração para múltiplas unidades por cidade), por pedido explícito do usuário. **3 fotos novas do usuário** localizadas no `img/`: `Campus II - Clínica Escola Veterinária (Campus Cruzeiro).jpg` (Umuarama Campus II), `Campus III - Campus Tiradentes.jpg` (Umuarama Campus III) e `TOLETO CAMPUS 1.jpg` (Toledo Campus I). Todas as fotos de unidade em uso foram **renomeadas** para o novo padrão (extensão original mantida): `campus-umuarama.jpg`→`unidade-umuarama-01.jpg`, `campus-cianorte.png`→`unidade-cianorte-01.png`, `campus-toledo.jpg`→`unidade-toledo-02.jpg`, `campus-guaira.jpg`→`unidade-guaira-01.jpg`, `campus-paranavai.jpg`→`unidade-paranavai-01.jpg`, `campus-cascavel.webp`→`unidade-cascavel-01.webp`, `campus-francisco-beltrao.png`→`unidade-francisco-beltrao-01.png`. **Cada um dos 10 campi agora tem foto própria** — Umuarama 01/02/03, Toledo 01 (foto nova) e 02; o Campus II de Umuarama deixou de ter `imagem: null` em `data/unidades.js`. Referências atualizadas em `index.html` (10 cards) e `data/unidades.js` (10 items); CSS/JS intactos. Imagens antigas sem uso mantidas (`campus-guaira.png`, `campus-umuarama.png`, `Sede_umuarama.jpg`). Layout, mapa, lista e filtros NÃO alterados.
- **2026-09-06:** Na faixa de parceiros do hero, os logos são **decorativos (não clicáveis)**, atualizando a intenção original de "cada parceiro clicável". Os parceiros seguem clicáveis na seção `#parceiros` existente. Mantém-se o hero atual (sem redesign full-screen). Confirmações do usuário registradas em "Decisões". *(Anotação histórica — a faixa foi posteriormente removida.)*

- **2026-09-07:** A decisão "mapa | lista de cidades + filtros abaixo, **sem cards na seção principal**" (2026-09-07) foi **substituída** pela estrutura **final combinada**: o bloco **PRESENÇA** (mapa + lista de cidades + filtros) permanece como está e, **abaixo** dele, foi adicionada a subseção **NOSSAS UNIDADES** com os **10 cards** de unidade (`.units__grid` + `.unit-card`), por **pedido explícito do usuário** (os dois blocos devem existir ao mesmo tempo; usuário confirmou substituir a decisão anterior). Alterados `index.html` (`.units__heading` + `.units__grid` + 10 cards) e `css/styles.css` (`.units__heading`, `.units__title`, `.units__grid`); `js/main.js`, `js/map.js` e `data/unidades.js` intactos. `specs/design.md` atualizado (componente "Cards de unidade" e decisão "Unidades").
- **2026-09-07:** A decisão "mapa sozinho + 10 cards das unidades abaixo" (2026-09-07) foi **substituída** pela estrutura **definitiva** "mapa | lista de cidades + filtros abaixo, sem cards na seção principal", por **pedido explícito do usuário (correção definitiva)**. Justificativa: a lateral do mapa deve conter somente os 7 nomes de cidades (uma vez cada) e os filtros ficam abaixo do bloco; dados das 10 unidades preservados para futuras subpáginas; CSS `.unit-card` mantido dormente. Alterados `index.html`, `css/styles.css`, `js/main.js`; `js/map.js` e `data/unidades.js` intactos. `specs/design.md` atualizado (componentes "Mapa das unidades", "Lista de cidades", "Filtros por cidade" e decisão "Unidades"). *(Anotação histórica — estrutura posteriormente substituída pela versão combinada acima.)*
- **2026-09-07:** A decisão de 2026-09-07 "Seção 'Presença e unidades' reorganizada (lista de cidades + filtros abaixo do mapa)" foi **substituída** pela nova estrutura "mapa sozinho + 10 cards das unidades abaixo", por pedido explícito do usuário, a fim de restaurar as imagens e informações das unidades que haviam sido removidas. A justificativa foi preservar a visibilidade das imagens, endereços e links das 10 unidades na home. Alterados `index.html`, `css/styles.css`, `js/main.js`; `js/map.js` e dados intactos.
- Sem outras mudanças oficiais até o momento. Qualquer substituição de decisão em `specs/design.md` deve ser registrada aqui com data e motivo, após confirmação do usuário.

## Histórico

- **2026-09-14:** Bloco final do **Sescoop Paraná** movido para a página da **Uniprime Pioneira** (com adaptações, por decisão do usuário). **Sescoop** (`projetos/sescoop-2027.html` + `_body-sescoop-2027.html`): removida a SECTION 7 "Educação que transforma / Formação e qualificação para o desenvolvimento do cooperativismo" (6 parágrafos + duo `sescoop-4`/`sescoop-5`). Nenhuma outra seção/texto/imagem do Sescoop alterada; CLOSING e "Voltar para projetos" intactos. **Aproveitado para corrigir título duplicado pré-existente** (`Sescoop Paraná | Unipar In Company | Unipar In Company` → `Sescoop Paraná | Unipar In Company`). **Uniprime** (`projetos/uniprime.html` + `_body-uniprime.html`): o bloco foi inserido como **último conteúdo** dentro da seção final "Excelência na gestão cooperativa / Fortalecimento técnico e evolução da gestão cooperativa" (após os 2 parágrafos originais e os 3 parágrafos de encerramento em `<strong>`), na ordem: 6 parágrafos **adaptados** para a realidade da Uniprime (parceiros corretos: **Fundação Cândido Garcia + Uniprime Pioneira + Sistema Ocepar**; treinamento "Análise de Crédito, Gestão de Risco, Conformidade Regulatória e Resolução 4966" com o professor **Cláudio Treinamentos**; removidas menções a Sescoop/Uniprime do Iguaçu/Valdenilson/"no Paraná") + **duo de panorâmicas** `uniprime-5` (esquerda) → `uniprime-6` (direita) reutilizando a classe escopada já existente **`.case-image-duo--sescoop`** (exibição integral, **sem alterar CSS**) — decisões confirmadas pelo usuário via questionário. **Arquivo renomeado:** `img/unipreme-5.jpg` → `img/uniprime-5.jpg` (correção da digitação). Páginas regeneradas via `build.ps1`; validadas (sync template↔página, tags balanceadas, sem referências órfãs a `sescoop-4/5`, `Valdenilson`, `Uniprime do Iguaçu`). `specs/design.md` atualizado (exceção `.case-image-duo--sescoop` agora também usada no duo de panorâmicas da Uniprime).

- **2026-09-14:** Na página da **Uniprime Pioneira** (`projetos/uniprime.html` e template `projetos/_body-uniprime.html`), os três textos de **encerramento** (que ficavam no era standalone `<!-- CLOSING -->` com `.project-closing`) foram **movidos para dentro da seção final "Excelência na gestão cooperativa"** (que contém o título "Fortalecimento técnico e evolução da gestão cooperativa"), por pedido explícito do usuário. Os três parágrafos passaram a ser novos `<p>` no mesmo `text-block__body` da seção, logo após os dois parágrafos existentes, com destaque via `<strong>` (exatamente no padrão solicitado pelo usuário; os textos não foram reescritos — apenas reposicionados). O `<section class="w-full">`/`.project-closing` standalone foi **removido** (sem duplicar conteúdo); "Voltar para projetos" continua sendo o último elemento; nenhuma outra seção, imagem, CSS, layout ou responsividade foi alterada. Página regenerada via `build.ps1` (`-Title "Uniprime Pioneira"`, sem sufixo, pois `_head.html` já adiciona "| Unipar In Company") para manter template e página em sincronia. `specs/design.md` não teve conflito registrado para esta mudança.

- **2026-09-14:** Corrigida a exibição das imagens **`sescoop-4.jpg`** (1280×482, 2.66:1) e **`sescoop-5.jpg`** (1280×551, 2.32:1) no duo do projeto **Sescoop Paraná** — antes eram renderizadas com `object-fit: cover` num box `16/10 → 21/10`, cortando bastante topo/base por serem fotos panorâmicas muito largas. **Solução (com confirmação do usuário — substituição da decisão "fotografias usam object-cover" apenas para estas duas):** classe escopada **`.case-image-duo--sescoop`** aplicada no wrapper do duo em `projetos/sescoop-2027.html` e template `projetos/_body-sescoop-2027.html`, com regras em `css/styles.css` — `.case-image-duo--sescoop .case-image { aspect-ratio: auto }` e `.case-image-duo--sescoop .case-image img { height: auto; object-fit: contain }`. Resultado: **imagem 100% visível, proporção original preservada, sem deformação, sem corte e sem áreas vazias artificiais**; duo continua lado a lado no desktop (grid ≥640px) e empilha no mobile; texto e CLOSING permanecem no lugar. **Escopo:** nenhum outro projeto/componente alterado (Gazin, Castelini etc. seguem com `cover`); `specs/design.md` atualizado (componente "Duo de imagens" e "Imagens e Mídia").

- **2026-09-11:** Corrigido o **Erro 153** que persistia nos 4 players do YouTube (`g6NU3KAg-Mk` na Gazin; `QW9xTLIZaDY`, `JLVEyRCE5ko`, `aRr75LKqbEc` no Sicredi). A causa era o parâmetro **`origin` hardcoded** (`origin=https%3A%2F%2Fwww.unipar.br`) adicionado em 2026-09-07, que nunca correspondia à origem real de visualização (site estático aberto via `file://`, localhost ou outro domínio). O YouTube valida `origin` contra a origem real da página hospedeira; a divergência dispara o erro 153. A solução: (1) **removido o `origin` fixo** dos 4 iframes em `projetos/gazin.html`, `projetos/sicredi.html` e nos templates `_body-gazin.html`/`_body-sicredi.html` (URL canônica `youtube-nocookie.com/embed/<ID>?rel=0`, IDs/ordem/`allow`/containers preservados); (2) **origem dinâmica** em `js/main.js` — ao servir via `http(s)`, injeta `origin=<location.origin>` real (localhost ou domínio do deploy); via `file://`, remove o `origin`. Validado: servidor local + checagem DOM (páginas 200, 4 iframes sem origin hardcoded), embeds no YouTube todos HTTP 200 e lógica de origem simulada no Node. Observação: abrir `file://` direto no Chrome continua bloqueando embeds YouTube por limitação do navegador — usar servidor local (`npx serve`/similar) ou a URL publicada. Nada mais alterado (layout, CSS, home, navegação intactos). *(Substitui a decisão de 2026-09-07 de fixar `origin` em `unipar.br`.)*
- **2026-09-09:** Adicionada na página da **Gazin** a nova seção **"Análise e concessão de crédito para pessoa jurídica"** (4 parágrafos, eyebrow "Concessão de crédito") com as imagens **`img/gazin-9.jpg`** e **`img/gazin-10.jpg`** lado a lado via componente existente **`.case-image-duo`** (sem alteração de CSS). Inserida entre SECTION 9 e SECTION 10 ("Educação corporativa que gera desenvolvimento"), de forma **aditiva** — nenhum conteúdo existente removido/alterado (CLOSING, vídeo "O projeto em movimento" e footer intactos). Alterados `projetos/gazin.html` e template `projetos/_body-gazin.html`. Imagens 1–8 intactas; nenhum asset criado; nenhuma dependência instalada.
- **2026-09-09:** Adicionadas as imagens **`castelini-5.jpg`** e **`castelini-6.jpg`** (já existentes em `img/`) lado a lado abaixo do bloco completo de texto do Programa de Formação de Gestores (entre SECTION 9 e CLOSING) em `projetos/castelini.html` e seu template `projetos/_body-castelini.html`. Nova DIV `.case-image-duo` (CSS Grid `1fr` → `2col 640px`, gap 20px) reutilizando `.case-image`/`.case-image--cover` existentes (border-radius 16px, shadow, aspect-ratio 16/10 → 21/10, `object-fit: cover`), com classe `reveal` (fade-up). Imagens 1–4 intactas; nenhuma imagem criada/baixada/substituída; nenhuma outra seção alterada. `css/styles.css` alterado (componente `.case-image-duo`). `specs/design.md` atualizado (componente "Duo de imagens"). *(Posteriormente reposicionadas: o duo foi movido para imediatamente abaixo do bloco "Aproximadamente 100 horas de aprendizado e evolução", antes da SECTION 8 — por pedido do usuário.)*
- **2026-09-07:** Corrigido o **Erro 153** ("Erro de configuração do player de vídeo") nos players do YouTube das **subpáginas de projeto** `projetos/gazin.html` e `projetos/sicredi.html` (e seus templates `_body-gazin.html` / `_body-sicredi.html`). O erro 153 do YouTube ocorre quando a requisição do player embed não identifica a origem (sem HTTP Referer identificável nem parâmetro `origin`). Os iframes já usavam `youtube-nocookie.com/embed/<ID>?rel=0` + `referrerpolicy="strict-origin-when-cross-origin"`; o fix aplicado foi **adicionar o parâmetro `origin`** ao URL do embed: `?rel=0&origin=https%3A%2F%2Fwww.unipar.br` (origin escolhida pelo usuário: unipar.br). Aplicado nas 4 ocorrências (Gazin: 1 vídeo `g6NU3KAg-Mk`; Sicredi: 3 vídeos `QW9xTLIZaDY`, `JLVEyRCE5ko`, `aRr75LKqbEc`), tanto nas páginas geradas quanto nos templates, para o `build.ps1` não reverter. **IDs originais mantidos**, `rel=0`, `referrerpolicy`, `allow`/`allowfullscreen` e containers (`.video-item__frame` 16/9 e 9/16) intactos. Validado: embeds retornam HTTP 200; DOM headless confirma iframe corretamente incorporado com `origin`; páginas e templates em sincronia. Nada mais alterado (layout, CSS visual, home e demais seções intactos).
- **2026-09-07:** Fotos das unidades padronizadas/renomeadas para **`img/unidade-<cidade>-<nn>.<ext>`** com **fotos novas do usuário** aplicadas a Umuarama Campus II (`unidade-umuarama-02.jpg`), Campus III (`unidade-umuarama-03.jpg`) e Toledo Campus I (`unidade-toledo-01.jpg`). Padrão: `unidade-<cidade>-<nn>.<ext>` (numeração original, extensão mantida); Umuarama 01/02/03, Cianorte 01, Toledo 01/02, Guaíra 01, Paranavaí 01, Cascavel 01, Francisco Beltrão 01. Referências atualizadas em `index.html` (10 cards) e `data/unidades.js` (10 items; Campus II de Umuarama deixou de ser `imagem: null`). Validado: `node --check` OK, nenhuma referência antiga (`campus-*`, `Sede_umuarama`, `TOLETO...`, `Campus Cruzeiro`, `Tiradentes`) no código, todas as 10 fotos existem em disco, estrutura da seção preservada (10 cards/10 imagens). Imagens antigas sem uso mantidas. Layout, mapa, lista de cidades, filtros e demais seções NÃO alterados.
- **2026-09-07:** Seção "Presença e unidades" — **estrutura final combinada**: bloco **PRESENÇA** (mapa à esquerda + lista de cidades à direita + filtros abaixo) e, **abaixo** dele, a subseção **"Nossas unidades"** (`.units__heading`: "Nossas unidades" / "Conheça nossos campi") com o **grid de 10 cards de unidade** (imagem, cidade, campus, endereço com pin, CTA "Mais informações" → links oficiais unipar.br `target="_blank"`). Distribuição Umuarama 3 / Cianorte 1 / Toledo 2 / Guaíra 1 / Paranavaí 1 / Cascavel 1 / Francisco Beltrão 1. **Substitui a decisão de 2026-09-07 "sem cards na seção principal"** (registrada em "Mudanças de Direção", com confirmação do usuário). Alterados `index.html`, `css/styles.css`; `js/main.js`, `js/map.js` e `data/unidades.js` intactos. Validado via `node --check`, dump DOM headless (10 cards, mapa+7 marcadores, 7 cidades, 8 chips, ordem mapa→lista→filtros→heading→cards) e medição de layout 375/768/1440 (sem overflow, grid 1col/2col/3col, cards abaixo do mapa). `specs/design.md` atualizado (componentes e decisão "Unidades").
- **2026-09-07:** Seção "Presença e unidades" **corrigida na estrutura definitiva**: mapa **à esquerda** + **lista de cidades à direita** (`.units__cities-card`, 7 nomes únicos, sem fotos/endereços/cards) + **filtros abaixo** (`.units__filters-row`, chips). Cards `.unit-card` e `.units__heading` **removidos da home**; dados dos 10 campi permanecem em `data/unidades.js` para futuras subpáginas `unidades/<cidade>.html`; CSS `.unit-card` mantido como componente dormente. **Substitui a decisão de 2026-09-07 "mapa sozinho + 10 cards abaixo"** (registrada em "Mudanças de Direção"). Alterados `index.html`, `css/styles.css`, `js/main.js`; `js/map.js` e dados intactos. Validado via `node --check` (JS OK), dump DOM headless (todas as checagens PASS) e medição de layout 375/1440 (sem overflow; desktop grid `1.4fr 1fr` + filtros abaixo; mobile empilhado). `specs/design.md` atualizado (componentes "Mapa das unidades", "Lista de cidades", "Filtros por cidade" e decisão "Unidades"). *(Anotação histórica — estrutura posteriormente substituída pela versão combinada acima.)*
- **2026-09-07:** Seção "Presença e unidades" **corrigida** — a lista de cidades e os filtros (da reorganização anterior) foram **removidos**; o mapa voltou a ficar **sozinho** no topo da seção (card centralizado) e os **10 cards de unidade/campus** foram **restaurados abaixo do mapa** (imagem, cidade, campus, endereço, CTA "Ver unidades" → página externa oficial unipar.br). **Substitui a decisão de 2026-09-07 "lista de cidades + filtros abaixo do mapa"** (registrada em "Mudanças de Direção" deste arquivo, com confirmação do usuário). Alterados `index.html`, `css/styles.css`, `js/main.js`; `js/map.js` e `data/unidades.js` intactos. `specs/design.md` atualizado (componente "Mapa das unidades" e decisão "Unidades").
- **2026-09-07:** Seção "Presença e unidades" **reorganizada**: lateral direita do mapa agora é uma **lista de cidades** (7 únicas, sem fotos/endereços); filtros movidos para **linha própria abaixo** do mapa+cidades; cards `.unit-card` removidos da seção principal; dados dos 10 campi preservados em `data/unidades.js` para futuras subpáginas. Alterados `index.html`, `css/styles.css`, `js/main.js`; `js/map.js` e dados intactos. `specs/design.md` atualizado (componente "Mapa das unidades").
- **2026-09-07:** Seção "Presença e unidades" — **mapa antigo restaurado** conforme a referência `unipar-main/components/sections/units.tsx`: título/subtítulo antigos, legenda sob o mapa, layout mapa à esquerda + filtro/cards à direita (`.units__layout`), estado `--red-50`/`--primary` 1.5, marcadores halo+pino com tooltip no hover/:focus/ativo, labels sempre visíveis removidos. Alterados `index.html`, `css/styles.css`, `js/map.js`; cards/imagens/dados/filtro/interação intactos. Verificado via dump DOM (20/20) e medição de layout calculado (800px e 1440px). `specs/design.md` atualizado (componente "Mapa das unidades").
- **2026-09-07:** Formulário de contato passou a **enviar de verdade via Resend** (Vercel Function `api/contact.js` + `js/contact.js` reescrito). Segredos só no servidor; validação dupla, honeypot e rate limit 5/10min por IP; CSS de erros de campo e honeypot adicionados. Criados `.env.example`, `.gitignore` e `package.json`. Harness local (fetch mockado): **42/42 testes OK**. `specs/design.md` atualizado (decisão de envio do formulário resolvida).
- **2026-09-07:** Card de **Umuarama** passou a usar a nova foto `campus-umuarama.jpg` (cópia idêntica de `Sede_umuarama.jpg`); referências atualizadas em `index.html` e `data/unidades.js`. Foto antiga `campus-umuarama.png` mantida sem uso.
- **2026-09-07:** Imagens das unidades padronizadas para `img/campus-<cidade>.<ext>` (extensão original mantida). Fotos novas aplicadas a Cascavel (`campus-cascavel.webp`), Paranavaí (`campus-paranavai.jpg` — card ganhou imagem) e Guaíra (`campus-guaira.jpg`); `campus-umuarama-3.png` → `campus-umuarama.png`. Referências atualizadas em `index.html` e `data/unidades.js`; ALT padrão "Campus Unipar <Cidade>". Imagens antigas mantidas sem uso (`campus-guaira.png`, `campus-guaira-2.png`, `Sede_umuarama.jpg`); corrigidas referências pré-existentes quebradas (`campus-cascavel.png`, `campus-umuarama-2.png`).
- **2026-09-07:** Deduplicou-se a seção "Nossas Unidades" na home — de 10 cards para **7 cards de cidade** (Umuarama, Cianorte, Toledo, Guaíra, Paranavaí, Cascavel, Francisco Beltrão), conteúdo de nível cidade, links apontando para futuras subpáginas `unidades/<cidade>.html`. Os **10 campi individuais foram preservados** em `data/unidades.js` (novo arquivo). Paranavaí sem imagem (inexistente no projeto). Mapa/filtro intactos. `specs/design.md` atualizado (decisão "Unidades" resolvida).
- **2026-09-06:** Análise completa do site estático existente (`index.html`, `css/styles.css`, `js/main.js`, `js/map.js`, `js/contact.js`, `img/`, `img/partners/`, `data/parana.geojson`, estrutura `projetos/`). Criação de `AGENTS.md`, `specs/design.md` e `memoria.md`. Nenhum arquivo do site existente foi alterado.